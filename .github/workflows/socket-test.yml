name: Polymarket Socket Test

on:
  workflow_dispatch:

jobs:
  socket-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install packages
        run: |
          pip install websockets

      - name: Run socket test
        run: |
          python find_btc_market.py
